#include<iostream>
#include<vector>
#include <algorithm>
using namespace std;
int main()
{

    int t;
    cin>>t;
    while(t--)
    {
        vector<int>v(3,0);
        for(int i=0; i<3; i++)
        {
            int x;
            cin>>x;
            v[i]=x;
        }
        sort(v.begin(),v.end());
        cout<<min(v[2]-v[0],v[1])<<endl;

    }

}